{
  "name": "@byhow/eslint-config-mono",
  "version": "0.1.0",
  "license": "MIT",
  "author": "byhow",
  "private": true,
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "lint": "npx eslint --ext .js,.json,.ts --fix .",
    "release": "lerna publish --conventional-commits"
  },
  "devDependencies": {
    "@byhow/eslint-config": "*",
    "eslint": "^8.8.0",
    "lerna": "^4.0.0",
    "typescript": "^4.5.5"
  }
}
